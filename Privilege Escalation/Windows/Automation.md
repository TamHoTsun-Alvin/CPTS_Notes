We can run WinPEAs to perform automated enumeration and check if exploit path is availble:

We first transfer WinPEAs to victim, then:

```
winPEAS.exe
```

We can also have it's output redirected to a file:

```
winPEAS.exe cmd > output.txt
```
